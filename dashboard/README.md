# Dashboard

Public web page showing current devices, flagged issues, and a
change timeline. This talks only to the backend API (`/latest`) —
never directly to anything on the home network.

Not started yet — planned for weeks 5-6 of the build. Pick whatever
frontend stack you're both comfortable with (plain HTML/JS is fine
for a hackathon demo; a framework only if you have time to spare).
